---
// src/layouts/BaseLayout.astro
const { frontmatter } = Astro.props;
---
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>{frontmatter.title} - Alex De Biasio</title>
	</head>
	<body>
		<header>
			<nav>
				<a href="/">Home</a>
				<a href="/blog">Essays</a>
				<a href="/projects">Projects</a>
				<a href="/cv">CV</a>
			</nav>
		</header>
		<main>
			<article>
				<h1>{frontmatter.title}</h1>
				<slot /> <!-- Your page content will be injected here -->
			</article>
		</main>
		<footer>
			© {new Date().getFullYear()} Alex De Biasio. All rights reserved.
		</footer>
	</body>
</html>

<style is:global>
	/* Import Google Fonts using the new variable font URL */
	@import url('https://fonts.googleapis.com/css2?family=Source+Sans+3:ital,wght@0,200..900;1,200..900&family=Source+Serif+4:ital,opsz,wght@0,8..60,200..900;1,8..60,200..900&display=swap');

	/* A modern CSS reset */
	:root {
		--accent: #2337ff;
		--accent-dark: #000d8a;
		--black: #1a1a1a;
		--gray: #666;
		--off-white: #f4f4f4;
	}
	html {
		/* Apply Source Sans 3 to all body text */
		font-family: 'Source Sans 3', system-ui, -apple-system, sans-serif;
		font-size: 16px;
		line-height: 1.6;
		background-color: var(--off-white);
		color: var(--black);
	}
	body {
		display: flex;
		flex-direction: column;
		min-height: 100vh;
		margin: 0;
	}
	main {
		width: 100%;
		max-width: 800px; /* Max width for readability */
		margin: 0 auto;
		padding: 2rem 1rem;
		flex-grow: 1;
	}
	header, footer {
		width: 100%;
		max-width: 800px;
		margin: 0 auto;
		padding: 1rem;
	}
	header nav {
		display: flex;
		gap: 1.5rem;
		border-bottom: 1px solid #ddd;
		padding-bottom: 1rem;
	}
	footer {
		text-align: center;
		color: var(--gray);
		font-size: 0.9em;
		padding-top: 1rem;
		border-top: 1px solid #ddd;
	}
	h1, h2, h3 {
		/* Apply Source Serif 4 to all headings */
		font-family: 'Source Serif 4', serif;
		line-height: 1.2;
		margin-top: 2em;
		margin-bottom: 0.5em;
	}
	p {
		margin-bottom: 1em;
	}
	a {
		color: var(--accent);
		text-decoration: none;
	}
	a:hover {
		color: var(--accent-dark);
		text-decoration: underline;
	}
</style>